# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                               controllers.Application.index
GET     /room/stream                    controllers.Application.stream(socketIdentifier)
GET     /room/filter/start              controllers.Application.startFilter(language: Option[String] ?= None, filter: Option[String] ?= None)
GET     /room/filter/stop               controllers.Application.stopFilter()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
